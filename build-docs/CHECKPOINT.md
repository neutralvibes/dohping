# dohping build checkpoint
updated: 2026-08-18T16:35+01:00
phase: 6
phase_status: complete
completed: phase1, phase2, phase3, phase4, phase5, phase6
next_action: round 17 (reflow-aware reclaim, DECISIONS #75) shipped — the user's real-terminal drag test is the gate; on FAIL, option B (revert #73) is pre-agreed. Run with DOHPING_DEBUG=/tmp/dohping-debug.log and correlate the log (crossing → reclaim in place → reclaimed) with the screen (must be ONE block). Unverified-forever candidates: TCP mode, --log-file, --timestamp-format rfc3339, darwin/windows binaries.

notes: User acceptance fixes (DECISIONS #50-52): (1) consecutive errors no longer re-enter error state — one line, duration updates (EventProbeError); (2) permission-class probe errors abort with exit 3 + hint (incl. ping "Operation not permitted" wrapped as EPERM); (3) MIN/MAX/AVG/FAILS left-aligned under headers (up line now byte-identical to spec §7.4). Window-mode corrections (2026-08-17): #53 window renders IN PLACE on the normal terminal — fixed-size block (header + last N events + live line), cursor-up + per-row clear-to-EOL only; NO alternate screen, NO cursor-home, NO clear-to-end-of-screen; Enter/Exit no-ops; block stays visible after exit. #54 explicit column reset: redraw emits `\x1b[<n>A\r` and rows are `\r\n`-separated (CUU preserves column; bare LF doesn't reset it — user's terminal showed fragmented rows). Regression test renders the stream through a mini terminal emulator and asserts the visible screen grid. All gates re-verified: 7/7 race-clean, vet/gofmt clean. dist/ rebuilt (linux-amd64 sha 999026ce…).
Acceptance round 2 (2026-08-17): #55 flags may appear on EITHER side of HOST (GNU-style permutation, `dohping HOST -c 5` now works); #56 plain-line live finalize ends `\r\n` and the exit summary is `\r`-prefixed + `\r\n`-terminated (summary no longer drifts right on the user's no-ONLCR terminal); #57 `-i`/`-t` accept bare seconds (`-i 5` = 5s) plus duration strings, with a helpful error hint. Regression tests: both-orders CLI cases, seconds parsing, and a no-ONLCR terminal emulator asserting the rendered summary is a clean left-aligned block. Gates: gofmt/vet clean, 7/7 race-clean, golangci-lint + staticcheck clean. dist/ rebuilt.
Acceptance round 3 (2026-08-17): #58 timestamp color is default intensity (was dim — too hard to read); #59 liveness animation — the live line shows a rising bar (`▁▃▅▇`, one frame per probe event) in the DURATION↔MIN separator cell (column 48); finalized/history and piped output stay plain (spec §7.4 byte-identical). Regression tests render through the terminal emulator (raw-stream glyph checks are wrong — DECISIONS #54 lesson). Gates: gofmt/vet clean, 7/7 race-clean, golangci-lint + staticcheck clean. dist/ rebuilt.
Acceptance round 4 (2026-08-17): #60 correction — the animation frame moved into the DURATION field's padding (column 47) so it floats with whitespace on both sides (was glued to MIN at col 48), and the frame now advances on a fixed 1-SECOND timer independent of probe cadence (was per-probe-event — too slow at long `--interval`). Piped/quiet runs: no ticker, no animation. Regression tests assert col 47 + separator space + Tick-driven advance in both displays. Gates: gofmt/vet clean, 7/7 race-clean, golangci-lint + staticcheck clean. dist/ rebuilt.
Acceptance round 5 (2026-08-17): #61 the 1-second tick ALSO refreshes DURATION from the wall clock (`now − statusStart`, same math as Finalize) — the counter counts up every second at any `--interval`, in step with the animation (was: updated only per probe event, so it jumped every 5s at `-i 5`). Piped output unaffected. Regression tests: tick advances duration in both displays (injected clock 1m → 2m). Gates: gofmt/vet clean, 7/7 race-clean, golangci-lint + staticcheck clean. dist/ rebuilt.
Security (2026-08-17): #62 docs-only (timing model note). #63 gosec 2.28.0 DONE (Phase-6 deferred tool): 0 issues. Real fixes: log file 0600 (G302, verified stat→600), TCP close error discarded (G104). Justified #nosec: G204 ping subprocess (exec, not shell; net.IP literal), G304 log path (operator's own --log-file). INSTALL: prebuilt GitHub release tarball, NOT `go install` (that compiles Anthropic/OpenAI SDKs and OOM'd the sandbox). Gates: gofmt/vet clean, 7/7 race-clean, gosec/golangci-lint/staticcheck clean. dist/ rebuilt.
Acceptance round 6 (2026-08-17): #64 terminal resize — window mode re-measures the terminal on EVERY redraw. HOST is the elastic column (content-fit with terminal cap, min 15 max 40, `…` truncation — user-approved choice); all column math is rune-based/cells (fixes a latent multibyte misalignment: pad/truncateHost were byte-based); the block's cursor-up and stale-clear math counts PHYSICAL rows, so lines wrapped below the 81-cell minimum stay a coherent non-interleaved stack. Platform-split per user requirement: Unix SIGWINCH fast path (existing signalx split); Windows has no SIGWINCH, but the 1-second tick re-measures → self-heals ≤1s. Regression tests render through the terminal emulator (now with DECAWM autowrap) at widths 120/81/60 asserting the visible screen: expansion, retraction, truncation, below-floor coherence across repeated redraws, stale-row clearing on grow-back. Real-PTY proof (scripts/pty-resize-probe.py): mid-run TIOCSWINSZ 60→100 → block 13→11 physical rows, exactly 2 stale rows cleared, clean screen. Gates: gofmt/vet clean, 7/7 race-clean, golangci-lint + staticcheck + gosec clean. dist/ rebuilt + republished.
Acceptance round 7 (2026-08-17): #65 plain live mode is width-aware for the single live line — the same physical-row primitive as the window block, reduced to one row's bookkeeping: Display tracks lastPhysRows, walks back to the true start before every live rewrite, and defensively clears rows the line no longer uses; the finalized-line path gets the same walk-back so scrollback lines never start mid-tail. HOST stays FIXED in plain mode (scrollback can't be re-laid-out; DECISIONS #9 invariant preserved). Piped/--no-live output byte-identical (script contract). Plain live mode also gained the SIGWINCH fast path (winchCh → disp.Tick() for both displays). LATENT BUG FIXED in the shared primitive: stale-row clear now resets the column first (`\x1b[1B\r\x1b[K`) — cursor-down preserves the column, so a non-blank last row (full window / wrapped live line) previously left stale text at the row's start. Regression tests: anchored live line at 60 cols across repeated redraws (no downward drift), grow-back clears the tail (emulator now models non-reflowing resize), finalize after a wrapped live line starts clean, full-window stale-clear, and the no-escape piped contract. Real-PTY proof (scripts/pty-resize-probe.py `plain`): fixed 60-col pty, live line anchored at row 2 across a 6s run. Gates: gofmt/vet clean, 7/7 race-clean, golangci-lint + staticcheck + gosec clean. dist/ rebuilt + republished.
Acceptance round 8 (2026-08-17): #66 reflowing-terminal re-anchor via DSR/CPR — the root fix for "It can creep up and does not clear the rest on wrap". On reflowing terminals (Windows Terminal / Terminal.app / iTerm2) a resize re-wraps existing lines, invalidating the #65 relative bookkeeping (and the #64 window anchor — same root cause as the earlier window creep). Fix: when the terminal width changes, query the cursor position (`\x1b[6n` → `\x1b[<r>;<c>R`); the cursor sits at the end of the re-wrapped line, so the anchor recomputes from the terminal's answer. The CPR row is compared against the bookkeeping's expectation — equal means no reflow (non-reflowing terminals behave byte-identically to #65); only genuine reflow re-anchors. Both displays: plain live line + window block. Plumbing: the app owns the query (key reader parses CSI, delivers CPR on a dedicated channel, 150ms timeout, degrade on no-answer); x/term's Windows MakeRaw sets ENABLE_VIRTUAL_TERMINAL_INPUT so CPR works on Windows Terminal too. Regression tests: reflow re-anchor for display + window (fake CPR answers), no-reflow-cursor-agrees keeps #65 behavior, malformed-CPR rejection, and the PTY probe now ANSWERS CPR from its live emulator cursor — plain/window scenarios are true end-to-end round-trip tests of the real binary. Gates: gofmt/vet clean, 7/7 race-clean, golangci-lint + staticcheck + gosec clean. dist/ rebuilt + republished.
Acceptance round 9 (2026-08-18): #67 CORRECTION — the #66 CPR re-anchor is REPLACED by freeze-and-restart. The user's terminal is Windows Terminal → WSL2 → Debian: the `\x1b[6n` answer comes from ConPTY, whose reflow differs from the rendered view (microsoft/terminal#18725 — "wildly incorrect cursor positions" on resize), so the CPR anchor was garbage and round 8 was still broken. Fix: displays never reclaim a reflowed rendering — on width change they FREEZE in-place writes (300ms settle; the clock restarts on every further change, so a drag extends the freeze), then `\r\n` to a fresh row below the frozen (re-wrapped) rendering and continue; the old rendering stays in scrollback as history. Position-independent, no terminal cooperation, fully sandbox-reproducible. CPR machinery deleted (reanchor/cprCh/parseCPR/CSI parsing; q/Ctrl-C parsing kept). Cost: one frozen line/block per resize + paused updates during a drag — accepted. Regression tests: freeze→restart (display + window), drag-keeps-freezing (5-step drag, one restart), finalize-mid-resize, grow-back keeps frozen content; PTY probe reworked (no CPR answering; window scenario asserts frozen block + fresh block below; raw-stream forensics: one restart CRLF, zero `\x1b[6n`). Gates: gofmt/vet clean, 7/7 race-clean, golangci-lint + staticcheck + gosec clean. dist/ rebuilt + republished.
Acceptance round 10 (2026-08-18): #68 (a) RESIZE MARK — when the settle restart leaves a frozen line behind, the display replaces its TIME with a single `-` (safe only when the line was one physical row before AND after the resize — then the frozen row is provably the row above the fresh line on every terminal; skipped when wrapped). (b) UNDER-80 RENAME — STATUS → STATE (5-wide field) and the never-established status renders `?` in the table (it genuinely appears during the hysteresis ramp — state.go carries the unchanged status on non-transition events); the word "unknown" survives in the exit summary. Minimum line 81 → 79; animation frame 47 → 45. Spec §7.4/§8.7 goldens, README, column tests, floor tests (81 → 79) all updated. Gates: gofmt/vet clean, 7/7 race-clean, golangci-lint + staticcheck + gosec clean. dist/ rebuilt + republished.
Acceptance round 11 (2026-08-18): USER CONFIRMED the #67/#68 freeze-and-restart on the real terminal, plain view — "the last change for the plain view appears to be working on resize and provides a much nicer visual". Frozen rows (marked `-`) stay in scrollback as history; the live line continues fresh below; repeated resizes stack one frozen row each (5 observed). The last open user gate for the resize work is now closed for plain mode (DECISIONS #69). No code change — evidence record only; no rebuild/republish.
Acceptance round 12 (2026-08-18): #70 CONDITIONAL freeze in WINDOW mode — the freeze fires only when the reflow would actually MOVE the block (any row of the last completed frame changes its physical row count at the new width; compare retained lastRows reflowed at newW vs lastPhysRows). Same-band resizes repaint IN PLACE with no artifact (user's terminal is ~55–60 cols — below the 79-cell floor — and every resize there froze under #67's unconditional trigger, stacking a frozen block per width change, screenshot-confirmed AVG 3.92 wrapping mid-value). Plain display UNCHANGED per user ("plain view is plain view"). User's column-trim idea parked — user may re-think window mode. Regression tests: same-band repaint/wrapped/drag (zero freezes), band-crossing still freezes, 120→79 retract now in place; PTY probe gains `window-same-band` (PASS) — window 60→100 + plain still PASS. Gates: gofmt/vet clean, 7/7 race-clean, golangci-lint + staticcheck + gosec clean. dist/ rebuilt + republished (sha 466b95c0…).
Acceptance round 13 (2026-08-18): #71 WINDOW-MODE COLUMN TRIM — below the 79-cell minimum the rightmost columns (FAILS→AVG→MAX→MIN) drop so the line fits instead of wrapping, down to essentials (46 cells). User: "my terminal doesn't live in any band, I am testing so use it accordingly" — the #70 conditional freeze only separates same-band from crossing, and free testing crosses constantly; trimming eliminates the crossings, so at ANY width ≥ 46 a resize is an in-place repaint with zero artifacts. Plain untouched. Freeze tests re-based to the 46-cell floor (40→100, 45→60, drag); PTY probe: window 60→100 now asserts ONE block (in-place), new window-subfloor 40→70 (freeze, two blocks). README updated. Gates: gofmt/vet clean, 7/7 race-clean, golangci-lint + staticcheck + gosec clean. dist/ rebuilt + republished (sha 5d79fd9b…).
Acceptance round 14 (2026-08-18): #72 USER ACCEPTED the trim ("This works much better") with a caveat — "sometimes it still wraps… This may have to be lived with, with a note specifying resizing terminals can cause output to fracture". README note added naming the three mechanisms: reflowing-terminal drag race (1s-tick sampling misses intermediate widths; the terminal reflows the old rendering regardless — transient fracture, self-correcting next repaint), sub-46 essentials wrap by design, and RTT ≥ 10,000 ms overflowing the 7-cell RTT fields (verified "10000.00" = 8 cells). Docs-only; optional future mitigations (RTT clamp, big-jump conservative freeze) recorded, not built.
Acceptance round 15 (2026-08-18): #73 SAME-BAND REPAINTS DEFERRED — after ANY width change the window writes nothing for the 300ms settle (each further change restarts the clock), then repaints in place. User's diagnosis: "it manages to wrap as a result of times events happen" + "when it wraps it creates another area to write to" — an event-timed redraw landing mid-reflow writes from a cursor that is a row off, shifting the block and claiming a new row. The defer removes the mid-reflow write entirely; no frozen artifact, no restart. Finalize forces the render. Residual (documented floor): ConPTY cursor-placement race after the reflow settles. Regression tests: same-band repaint/wrapped/drag + 120→79 retract now assert defer-then-repaint; crossing tests unchanged. Gates: gofmt/vet clean, 7/7 race-clean, golangci-lint + staticcheck + gosec clean. dist/ rebuilt + republished (sha 44c82b29…).
Acceptance round 16 (2026-08-18): #74 DEBUG LOGGING FACILITY — new internal/debugx package, enabled only by DOHPING_DEBUG=<path> (or SetWriter in code/tests); file-only (the display owns the terminal), 0600, RFC3339-ms [tag] lines; a path that cannot be opened disables with a warning, never breaks a run. Hook points are the app's own width observations: display/winch/tick/resize/redraw tags cover mode selection, SIGWINCH receipt, the 1s repaint, every width change with the freeze/defer decision (physical-row counts), and suppressed/deferred/released/restarted repaints + the settle repaint's phys span (a shifted block = span mismatch). User's point: no terminal displays the width while resizing, so a drag's sweep is reconstructable only from the app's log — "perhaps we should have had a facility for a debug logger already, just only enabled by code or ENV". End-to-end proof via the PTY probe (window-same-band): winch → resize 60→55 rows 11→11 → defer → two suppressed redraws → defer released → repainted tw=55 phys=11 (was 11). gosec: G703 annotated (taint twin of G304, same operator boundary as --log-file). Regression tests: debugx unit suite + window/display resize forensics. Gates: gofmt/vet clean, 7/7 race-clean, golangci-lint + staticcheck + gosec clean. dist/ rebuilt + republished.
Acceptance round 17 (2026-08-18): #75 REFLOW-AWARE IN-PLACE RECLAIM (user-approved option A, SPEC-window-resize-reclaim.md; B = revert #73 pre-agreed on failure). A settled crossing above the essentials floor is reclaimed in place — R = reflowedSpan(lastRows, tw), walk back R−1, overwrite with the fresh trimmed frame, clear R−N stale rows; no frozen copy, no restart, no scrollback reliance (§8.5 satisfied). Freeze remains only below the floor (fresh frame wraps) or when R > th; Finalize reclaims via forceRender. Same-band defer (#73) unchanged. The #74 debug log: crossing → reclaim in place (R/N/tw) → reclaimed (phys was). termScreen gains logical-line tracking + reflowResize (the reflowing-terminal model); crossing tests re-based to it (incl. the user's 97→75 stale-frame case → one block); below-floor settle freeze test (60→40, two blocks); PTY probe window-subfloor → 50→40 (settles below the floor, deterministic on the non-reflowing sandbox pty) — all 4 probe scenarios PASS. Non-reflowing crossing reclaim = documented limitation (world is undetectable; CPR lies on ConPTY). Gates: gofmt/vet clean, 7/7 race-clean, golangci-lint + staticcheck + gosec clean. dist/ rebuilt + republished (linux-amd64 sha 8dcce21a…). Gate: the user's real-terminal drag test.
Acceptance round 19 (2026-08-18): #78 THE RECLAIM IS SHIPPED. The round-18 rejection was based on a stale binary (unversioned-filename trap — fixed: versioned publish names + INDEX.txt). Proper retest of the genuine build (8dcce21a, byte-reproduced): user "holds up really well but is not faultless, which I think I will probably have to accept"; B rejected on direct test ("not what was promised but more of the same problem"); "This one should be set as the shipped build." Rounds 15+17 restored via revert-of-reverts; code byte-identical to 1f31f7b; release.sh reproduces the wear-tested build exactly (8dcce21a). Documented residual: mid-drag terminal reflow of the stale rendering (transient); possible one-row settle offset self-correcting next repaint. Gates: gofmt/vet clean, 7/7 race-clean, golangci-lint + staticcheck + gosec clean. dist/ rebuilt + republished (linux-amd64 sha 8dcce21a…, plain name updated).
